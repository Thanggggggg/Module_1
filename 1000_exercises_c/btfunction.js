function generateRandomArray() {
    let arr = [];
    for (let i = 0; i < 10; i++) {
      let randomNum = Math.floor(Math.random() * 10) + 1;
    }
    console.log(generateRandomArray());
  }
  sử dụng kiến thức hàm
  tạo 1 hàm tạo mảng, hiển thị mảng ra mỗi giá trị trên 1 hàng, tìm số lớn nhất trong mảng
